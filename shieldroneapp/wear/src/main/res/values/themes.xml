<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Shieldrone" parent="android:Theme.Material.Light.NoActionBar">
        <!-- 기본 테마 설정 -->
    </style>

    <style name="Theme.Shieldrone.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/shieldrone_ic</item>
        <item name="postSplashScreenTheme">@android:style/Theme.DeviceDefault</item>
    </style>
</resources>